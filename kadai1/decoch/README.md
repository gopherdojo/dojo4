# 画像変換コマンドの実装

## Usage
```
./converter [options...] PATH

OPTIONS:
  --input value, -i value     a image extension converted from (default: .jpg)
  --output value, -o value    a image extension converted to (default: .png)
```